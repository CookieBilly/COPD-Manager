<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ws.billy.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Untreasured">
        <activity android:name=".Code.New.Login.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Code.New.Login.Signup" />
        <activity android:name=".Code.New.Questions.QuestionFragment" />
        <activity android:name=".Code.New.History.HistoryActivity" />
        <activity android:name=".InsideActivity" />
        <activity android:name=".Code.New.Chat.ChatActivity"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>